name: ewooral_icons
description: Ewooral & BFAM Holdings — distinctive medallion-style icon set with cartoonish splash on play. Configurable trigger (hover/tap/focus/mount/manual). 56 icons, growing.
version: 0.1.0
homepage: https://icons.ewooral.com
repository: https://github.com/Ewooral/ewooral-icons
issue_tracker: https://github.com/Ewooral/ewooral-icons/issues
topics: [icons, ui, animation, svg, brand]

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets: []
